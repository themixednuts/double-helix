# Pending Registry Entries

These packages are intentionally not seeded in wave 1 because they require npm,
pip, cargo, go, or a more specialized installer backend:

- `pyright` / `basedpyright` - npm
- `typescript-language-server` - npm
- `bash-language-server` - npm
- `yaml-language-server` - npm
- `debugpy` - pip

